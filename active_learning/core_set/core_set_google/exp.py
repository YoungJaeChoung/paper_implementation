import run_experiment as re


if __name__=="__main__":
    X
    y
    sampler
    score_model
    seed = 2019
    warmstart_size
    batch_size
    results, sampler_states = re.generate_one_curve(X, y, samppler, score_model, seed, warmstart_size, batch_size)
